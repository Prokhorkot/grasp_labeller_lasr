# Top-level dependencies for this project.
# Pins are kept from the known-working environment; transitive packages from
# pip freeze are intentionally omitted to keep the file readable.

# Local Sparsh encoder package.
# If editable install support causes trouble, install dependencies first, then run:
#   pip install --config-settings editable_mode=compat -e ./external/sparsh
-e ./external/sparsh

# Core ML stack.
torch==2.2.2
torchvision==0.17.2
lightning==2.4.0
torchmetrics==1.9.0

# Data loading and preprocessing.
numpy==1.26.0
pandas==2.2.3
h5py==3.12.1
opencv-python-headless==4.10.0.84

# Configuration and runtime helpers.
hydra-core==1.3.2
omegaconf==2.3.0
python-dotenv==1.2.2
rich==13.9.4
tqdm==4.68.3

# Sparsh model loading.
einops==0.8.0
huggingface-hub==0.26.5

# Experiment tracking.
dagshub==0.7.0
mlflow==3.14.0

# Notebook and plotting workflow.
ipykernel==7.3.0
ipython==9.15.0
matplotlib==3.9.2
pillow==10.4.0
